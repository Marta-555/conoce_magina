{% extends 'base.html.twig' %}

{% block title %}Hello PerfilController!{% endblock %}

{% block body %}

<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">

    {% if errorEmail is defined %}
        <h1> {{ controller_name }}</h1>
        {% for flash in app.flashes('error') %}
            <div class="alert alert-danger">{{ flash }}</div>
        {% endfor %}

    {% else %}
        {% for flash in app.flashes('success') %}
            <div class="alert alert-success">{{ flash }}</div>
        {% endfor %}

        <h1>Hello {{ controller_name }}! ✅</h1>

        This friendly message is coming from:
        <ul>
            <li>Your controller at <code><a href="{{ 'C:/wamp64/www/PROYECTO_INTEGRADO/conoce_magina/src/Controller/PerfilController.php'|file_link(0) }}">src/Controller/PerfilController.php</a></code></li>
            <li>Your template at <code><a href="{{ 'C:/wamp64/www/PROYECTO_INTEGRADO/conoce_magina/templates/perfil/index.html.twig'|file_link(0) }}">templates/perfil/index.html.twig</a></code></li>
        </ul>
    {% endif %}

</div>
{% endblock %}
